name: bl20-sequencer (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement status
        run: |
          echo "This workflow is retired."
          echo "BL-20 sequencing is now governed via claim-based work packages and OpenClaw runbooks (see #220/#224)."
