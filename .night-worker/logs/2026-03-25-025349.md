# Night Worker Log — 2026-03-25 02:53:49 (Europe/Berlin)

## Kontext / Entscheidungen
- Worktree wurde zuerst bereinigt (lokaler Zwischenstand in `git stash` gesichert), weil der aktive Branch hinter `origin/main` war und lokale Diffs teilweise gegen bereits gemergte Mainline-Änderungen liefen (Regression-Risiko).
- Für neuen Strang wurde ein sauberer Branch von `origin/main` erstellt: `night/worker-20260325-0300`.
- Fokus mit höchstem ROI: Deploy-Smoke-Matrix für Login-Start auf **Alias-Host** (`dev.geo-ranking.ch`) zusätzlich in CI absichern, nicht nur punktueller Canonical-Redirect-Check für `/gui`.

## Umgesetzte Änderungen
- Datei: `.github/workflows/deploy.yml`
  - Neue CI-Deploy-Step hinzugefügt:
    - `Smoke-Test UI login start route matrix on alias host (dev.geo-ranking.ch)`
    - Leitet Alias-Base-URL robust aus `UI_CANONICAL_ORIGIN`/`UI_CANONICAL_HOSTS` ab (mit Fallback/Skip bei fehlendem Alias).
    - Führt `scripts/smoke/run_login_start_smoke_bundle.sh` mit `--env-name dev-alias` aus.
  - Artifact-Upload erweitert um `dev-alias-login-start-smoke*.json` Dateien (alle Routen der Matrix).

## Live-Verifikation (gegen dev)
- Deploy-Run `23520856870` geprüft: **success** (Build/Test + vollständiger ECS-Deploy inkl. Smokes).
- Zusätzliche manuelle Route-Matrix-Smokes lokal gegen beide Hosts ausgeführt:
  - `https://www.dev.georanking.ch`
  - `https://www.dev.geo-ranking.ch`
- Ergebnis: beide Bundles erfolgreich (302 -> authorize) für alle gepflegten Routen inkl. Legacy-Pfade.
- Evidence lokal erzeugt unter `artifacts/nightworker/20260325T0305Z/`.

## Blocker / Hinweise
- Browser-Tool-Startup war in dieser Session einmalig timeout-behaftet; daher UI-Nähe über HTTP-Smoke-Skripte verifiziert (robust/reproduzierbar für CI).
- Verbotene WIP-Dateien wurden nicht verändert:
  - `reports/consistency_report.json`
  - `reports/consistency_report.md`
  - `triage_add_labels.sh`

## Nächster sinnvoller Schritt
- Commit + Push des Workflow-Pakets auf `night/worker-20260325-0300` und optional PR auf `main` mit Fokus „Alias-Route-Matrix im Deploy-Workflow“.
