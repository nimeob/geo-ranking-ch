# Night Worker Log — 2026-03-22 06:30 CET

## Kontext
- Repo: `geo-ranking-ch`
- Ziel: ROI-orientierter UI-/Smoke-Fortschritt auf DEV (`https://www.dev.georanking.ch`)

## Beobachtung (reproduziert)
1. Live-Smoke (`scripts/run_dev_ui_auth_analyze_smoke.mjs`) auf `/gui` grün.
2. Bei `DEV_UI_SMOKE_GUI_PATH=/gui/history` harter Timeout auf `#analyze-form` (false negative, weil History-Route kein Analyze-Form rendert).
3. Bei `DEV_UI_SMOKE_GUI_PATH=/gui/jobs` zusätzlicher Timeout im Login-Return-Check, weil DEV korrekt auf kanonischen Pfad `/jobs` umleitet.

## Entscheidung
- Script robust machen statt Testscope zu verkleinern:
  - Return-Path-Validierung auf kanonischen Successor erweitern (`/gui/jobs` → `/jobs`).
  - Analyze-Shell-Recovery ergänzen: wenn Zielroute kein Analyze-Form hat, nach validiertem Return-Path kontrolliert auf `/gui` wechseln und dort Analyze-Checks fortsetzen.
  - Backward-Kompatibilität bei Check-Key beibehalten (`loginReturnedToGui` Alias).

## Verifikation
- Guard-Tests + Doku-Guards grün:
  - `pytest -q tests/test_run_dev_ui_auth_analyze_smoke_script_contract.py tests/test_gui_dev_live_auth_analyze_smoke_docs.py tests/test_markdown_links.py`
- DEV Live-Retest grün:
  - `DEV_UI_SMOKE_GUI_PATH=/gui/history` ✅
  - `DEV_UI_SMOKE_GUI_PATH=/gui/jobs` ✅
- Evidence-Dateien unter `reports/evidence/dev-ui-auth-analyze-smoke-20260322T054526Z.*` und `...054640Z.*`.
