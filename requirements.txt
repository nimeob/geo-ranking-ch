# Runtime-Abhängigkeiten
# Core
requests==2.31.0
cachetools==5.3.3
tenacity==8.2.3

# Authentifizierung (JWT-Validierung)
python-jose[cryptography]==3.3.0

# Datenbank (optional)
psycopg2-binary==2.9.9

# AWS SDK (KORREKTUR: 2.x existiert nicht, 1.x ist aktuell)
boto3>=1.34.158,<2.0
